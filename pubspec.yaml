name: tictactoe_firebase
description: A TicTacToe game with Firebase (online/offline)

publish_to: 'none' # ถ้าไม่ใช่แพ็กเกจที่จะอัพขึ้น pub.dev ให้ใส่ none

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  firebase_core: ^3.5.0
  cloud_firestore: ^5.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
